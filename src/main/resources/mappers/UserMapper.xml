<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.keephealthy02.Mapper.UserMapper">

    <resultMap id="BaseResultMap" type="org.example.keephealthy02.Entity.User">
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="sex" jdbcType="INTEGER" property="sex"/>
        <result column="birthday" jdbcType="DATE" property="birthday"/>
        <result column="height" jdbcType="FLOAT" property="height"/>
        <result column="weight" jdbcType="FLOAT" property="weight"/>
        <result column="targetweight" jdbcType="VARCHAR" property="targetweight"/>
        <result column="past_Medical_History" jdbcType="VARCHAR" property="pastMedicalHistory"/>
    </resultMap>

    <delete id="deleteByPrimaryKey" parameterType="String">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <insert id="insert" parameterType="org.example.keephealthy02.Entity.User">
        INSERT INTO user (id, name, password, sex, birthday, height, weight, targetweight, past_Medical_History)
        VALUES (#{id}, #{name}, #{password}, #{sex}, #{birthday}, #{height}, #{weight}, #{targetweight}, #{pastMedicalHistory})
    </insert>

    <insert id="insertSelective" parameterType="org.example.keephealthy02.Entity.User">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="password != null">password,</if>
            <if test="sex != null">sex,</if>
            <if test="birthday != null">birthday,</if>
            <if test="height != null">height,</if>
            <if test="weight != null">weight,</if>
            <if test="targetweight != null">targetweight,</if>
            <if test="pastMedicalHistory != null">pastMedicalHistory,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null">#{name},</if>
            <if test="password != null">#{password},</if>
            <if test="sex != null">#{sex},</if>
            <if test="birthday != null">#{birthday},</if>
            <if test="height != null">#{height},</if>
            <if test="weight != null">#{weight},</if>
            <if test="targetweight != null">#{targetweight},</if>
            <if test="pastMedicalHistory != null">#{pastMedicalHistory},</if>
        </trim>
    </insert>

    <select id="selectByPrimaryKey" parameterType="String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id}
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="org.example.keephealthy02.Entity.User">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="password != null">password = #{password},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="height != null">height = #{height},</if>
            <if test="weight != null">weight = #{weight},</if>
            <if test="targetweight != null">targetweight = #{targetweight},</if>
            <if test="pastMedicalHistory != null">pastMedicalHistory = #{pastMedicalHistory},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateByPrimaryKey" parameterType="org.example.keephealthy02.Entity.User">
        UPDATE user
        SET name = #{name},
            password = #{password},
            sex = #{sex},
            birthday = #{birthday},
            height = #{height},
            weight = #{weight},
            targetweight = #{targetweight},
            past_medical_history = #{pastMedicalHistory}
        WHERE id = #{id}
    </update>

</mapper>
